# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Environment variables (never commit API keys)
.env
.env.local
.env.*.local

# Persistent memory data (JSON files with research sessions, skills, proposals)
# Do NOT ignore this directory — it contains accumulated knowledge.
# Uncomment below to exclude from git if data volume becomes large:
# data/sessions/*.json
# data/skills/*.json
data/*.tmp.*
